
# See the docstring in versioneer.py for instructions. Note that you must
# re-run 'versioneer.py setup' after changing this section, and commit the
# resulting files.

[versioneer]
VCS = git
style = pep440
versionfile_source = multi_loc/_version.py
versionfile_build = multi_loc/_version.py
tag_prefix = v
parentdir_prefix = multi_loc-
